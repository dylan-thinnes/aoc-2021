#!/usr/bin/env bash
case "$1 $2" in
  "1 " | "1 nix")
    nix eval -f nix/sol1 gt-last-count
    ;;
  "2 " | "2 nix")
    nix eval -f nix/sol2 gt-last-count
    ;;
  "1 j")
    cd j
    cat sol1.j | ijconsole
    ;;
  *)
    echo "Unrecognized solution '$1 $2'" 1>&2
    exit 1
    ;;
esac
